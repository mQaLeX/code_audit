# 项目架构说明

## 整体架构

本项目采用多Agent架构，主要包含以下组件：

```
┌─────────────────────────────────────────────────────────────┐
│                         主程序入口                            │
│                         main.py                             │
└────────────────────────┬────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ 函数扫描模块  │  │ 代码审计Agent│  │ 漏洞利用Agent│
│FunctionScanner│  │  AuditAgent  │  │ ExploitAgent │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  扫描脚本    │  │  LLM调用      │  │  LLM调用      │
│  或知识库    │  │  LLMClient   │  │  LLMClient   │
└──────────────┘  └──────────────┘  └──────────────┘
                                           │
                                           ▼
                                  ┌──────────────┐
                                  │ 报告生成Agent│
                                  │ ReportAgent  │
                                  └──────┬───────┘
                                         │
                                         ▼
                                  ┌──────────────┐
                                  │  安全报告    │
                                  │ (MD/JSON/HTML)│
                                  └──────────────┘
```

## 核心模块说明

### 1. 主程序入口 (main.py)

负责：
- 解析命令行参数
- 初始化各模块
- 协调各Agent的工作流程
- 输出执行结果

### 2. 工具模块 (utils/)

#### llm_client.py
- 封装OpenAI API调用
- 提供聊天完成接口
- 支持JSON响应解析

#### models.py
- 定义数据模型
- FunctionInfo: 函数信息
- AuditTask: 审计任务
- AuditResult: 审计结果
- ExploitResult: 利用结果
- VulnerabilityReport: 漏洞报告

### 3. 扫描模块 (scanners/)

#### function_scanner.py
- 统一的函数扫描接口
- 支持两种扫描方式：
  1. 预定义的扫描脚本
  2. 基于知识库的LLM生成脚本
- 支持多种项目类型和攻击面

#### scan_python_web.py
- Python Web应用专用扫描器
- 识别Flask、FastAPI、Django等框架的路由函数

#### scan_python_cli.py
- Python CLI应用专用扫描器
- 识别Click、Typer、argparse等框架的命令函数

### 4. Agent模块 (agents/)

#### audit_agent.py
- 代码审计Agent
- 创建审计任务
- 并发执行审计
- 调用LLM分析代码安全性

#### exploit_agent.py
- 漏洞利用Agent
- 生成利用脚本
- 执行利用脚本
- 判断利用是否成功
- 可选截图功能

#### report_agent.py
- 报告生成Agent
- 生成Markdown报告
- 生成JSON报告
- 生成HTML报告
- 计算CVSS评分
- 生成漏洞影响描述

### 5. 知识库 (knowledge/)

#### python_web.txt
- Python Web应用接口函数定义知识
- 常见安全关注点

#### python_cli.txt
- Python CLI应用接口函数定义知识
- 常见安全关注点

#### python_protobuf.txt
- Python Protobuf接口函数定义知识
- 常见安全关注点

#### python_blink.txt
- Python Blink引擎接口函数定义知识
- 常见安全关注点

#### audit_questions.json
- 不同攻击面的审计问题配置
- 包含漏洞类型和审计问题

### 6. 工具目录 (tools/)

#### exploit_scripts/
- 存储生成的漏洞利用脚本

#### screenshots/
- 存储漏洞利用截图

### 7. 报告目录 (reports/)

- 存储生成的安全报告

## 工作流程

### 阶段1: 函数扫描

```
输入: 代码目录、项目类型、攻击面
  │
  ├─→ 检查是否有预定义扫描脚本
  │   ├─→ 有: 直接调用脚本扫描
  │   └─→ 无: 加载知识库
  │       ├─→ 调用LLM生成扫描脚本
  │       └─→ 执行生成的脚本
  │
输出: 接口函数列表
```

### 阶段2: 创建审计任务

```
输入: 接口函数列表、攻击面
  │
  ├─→ 加载审计问题配置
  ├─→ 为每个函数创建审计任务
  │   └─→ n个函数 × m个问题 = n×m个任务
  │
输出: 审计任务列表
```

### 阶段3: 并发审计

```
输入: 审计任务列表
  │
  ├─→ 创建线程池
  ├─→ 并发执行审计任务
  │   ├─→ 调用LLM分析代码
  │   ├─→ 判断是否存在漏洞
  │   └─→ 返回审计结果
  │
输出: 审计结果列表
```

### 阶段4: 漏洞利用

```
输入: 确认有漏洞的审计结果
  │
  ├─→ 对每个漏洞生成利用脚本
  │   ├─→ 调用LLM生成Python脚本
  │   ├─→ 保存脚本到文件
  │   └─→ 执行脚本
  ├─→ 判断利用是否成功
  │   ├─→ 分析脚本输出
  │   └─→ 调用LLM辅助判断
  └─→ 可选截图
  │
输出: 利用结果列表
```

### 阶段5: 报告生成

```
输入: 审计结果、利用结果
  │
  ├─→ 汇总漏洞信息
  ├─→ 计算CVSS评分
  ├─→ 生成漏洞影响描述
  ├─→ 生成Markdown报告
  ├─→ 生成JSON报告
  └─→ 生成HTML报告
  │
输出: 安全报告文件
```

## 扩展指南

### 添加新的项目类型

1. 在`utils/models.py`中添加新的ProjectType枚举值
2. 在`knowledge/`目录下创建对应的知识库文件
3. 在`scanners/`目录下创建对应的扫描脚本（可选）

### 添加新的攻击面

1. 在`utils/models.py`中添加新的AttackSurface枚举值
2. 在`knowledge/`目录下创建对应的知识库文件
3. 在`knowledge/audit_questions.json`中添加对应的审计问题
4. 在`scanners/`目录下创建对应的扫描脚本（可选）

### 添加新的审计问题

编辑`knowledge/audit_questions.json`文件，在对应的攻击面下添加新的问题：

```json
{
  "web": [
    {
      "type": "新漏洞类型",
      "question": "审计问题描述"
    }
  ]
}
```

### 添加自定义利用工具

1. 在`tools/`目录下创建自定义工具脚本
2. 在`exploit_agent.py`中调用该工具

## 技术栈

- **Python 3.8+**
- **OpenAI API** - LLM调用
- **concurrent.futures** - 并发执行
- **ast** - Python代码解析
- **subprocess** - 脚本执行
- **json** - 数据序列化

## 设计原则

1. **模块化**: 每个模块职责单一，易于维护
2. **可扩展**: 支持添加新的项目类型、攻击面、审计问题
3. **并发**: 使用线程池提高审计效率
4. **灵活**: 支持多种扫描方式和报告格式
5. **安全**: 不使用LangChain，直接调用OpenAI API

## 注意事项

1. 所有LLM调用都应包含错误处理
2. 漏洞利用脚本应在受控环境中执行
3. 敏感信息（API密钥）应通过环境变量配置
4. 报告生成应包含详细的漏洞信息和修复建议
5. 并发数应根据API限制和系统资源合理设置
